<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8 bg-white p-4 rounded shadow-lg">
    <div>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        Resend confirmation instructions
      </h2>
    </div>
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <div class="rounded-md shadow-sm -space-y-px">
        <%= f.input :email, require: true, autofocus: true, placeholder: 'Email address', input_html: { autocomplete: "email", class: 'form-input' }, label_html: { class: "sr-only" } %>
        <%= f.button :button, type: :submit, class: 'group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm !font-medium rounded-md text-white bg-green-700 hover:bg-green-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-900' do %>
          <span class="absolute left-0 inset-y-0 flex items-center pl-3">
            <svg class="h-5 w-5 text-green-700 group-hover:text-green-900" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207" />
            </svg>
          </span>
          Send me reset password instructions
        <% end %>
      </div>
    <% end %>
    <div class="flex justify-end !mt-2">
      <%= link_to "Sign in", new_user_session_path, class: 'text-sm font-medium text-green-700 hover:text-green-900 mr-2' %>
    </div>
  </div>
</div>
