<div class="mb-1">
  <span class="text-sm text-gray-500 <%= 'font-12p' unless download %>"><%= date %></span>
  <p class="font-bold text-sm text-gray-900 <%= 'font-12p' unless download %>"><%= education.degree %></p>
  <span class="text-sm text-gray-700 <%= 'font-12p' unless download %>"><%= school.name %> - <%= school.city %></span>
  <div class="text-sm text-gray-700 <%= 'font-10p' unless download %>"><%= education.description %></div>
</div>
